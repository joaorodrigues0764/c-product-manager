# Regression Tests

These regression tests cover the current version of the project, including
product management, cart operations, invoicing, client filtering, validation,
and client NIF updates.

Run the complete suite from the repository root with:

```bash
make test
```
